name: Build Windows Executable

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Allows manual triggering from GitHub UI

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r audio_downloader_requirements.txt
        pip install pyinstaller pillow
        
    - name: Create icon
      run: python create_icon.py
        
    - name: Build executable
      run: pyinstaller --onefile --windowed --icon=icon.ico --name="Audio Downloader" audio_downloader_gui.py
        
    - name: Upload executable
      uses: actions/upload-artifact@v2
      with:
        name: Audio-Downloader-Windows
        path: dist/Audio Downloader.exe
        
    - name: Create release ZIP
      run: |
        cd dist
        powershell Compress-Archive -Path "Audio Downloader.exe" -DestinationPath "Audio-Downloader-Windows.zip"
        
    - name: Upload release ZIP
      uses: actions/upload-artifact@v2
      with:
        name: Audio-Downloader-Windows-ZIP
        path: dist/Audio-Downloader-Windows.zip 